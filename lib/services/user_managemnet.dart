class UserManagement {
  storeNewUser(user, context) {
    /*FirebaseFirestore.instance
        .collection('/users')
        .add({})
        .then(() {})
        .catchError((e) {
          print(e);
        });*/
  }
}
